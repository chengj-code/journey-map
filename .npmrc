# 强制使用 pnpm 包管理器
package-manager-strict=true

# 使用 pnpm 的 lockfile
lockfile=true

# 保存精确版本
save-exact=false

# 自动安装 peer dependencies
auto-install-peers=true

# 忽略脚本（可选，根据需要开启）
# ignore-scripts=false
